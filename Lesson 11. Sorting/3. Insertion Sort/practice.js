const insertionSort = function (arr) {
    // Iterate through the array starting from the second element
   
      // Store the current element to be inserted in the sorted portion

      // Initialize `j` to be the index of the previous element

  
      // TODO: Move elements of the sorted portion that are greater than `current`
      // to one position ahead of their current position
  
      // TODO: Insert the `current` element into its correct position
  
    // TODO: Return the sorted array

  };
  
  const unsortedArray = [5, 2, 4, 6, 1, 3];
  console.log(insertionSort(unsortedArray));
  