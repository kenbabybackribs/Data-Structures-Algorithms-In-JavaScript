const selectionSort = function (arr) {
  // TODO: Iterate over each element in the array
  // TODO: Assume the minimum element is at the current index
  // TODO: Iterate over the remaining unsorted portion of the array
  // TODO: If a smaller element is found, update the minIndex
  // TODO: Swap the found minimum element with the element at index `i`
  // TODO: Store the value at index `i` in a temporary variable
  // TODO: Move the minimum value to index `i`
  // TODO: Move the value from `minIndex` to index `i`
};

// Example usage:
const unsortedArray = [4, 2, 6, 5, 1, 3];
console.log(selectionSort(unsortedArray)); // Output: [1, 2, 3, 4, 5, 6]
